const formTheme = [
  {
    name: "name",
    type: "text",
    required: true,
    messageError: "Theme's name is required!",
    placeHolder: "Your theme's name",
  },
];

export default formTheme;
